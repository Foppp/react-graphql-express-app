import { makeStyles } from '@mui/styles';

export default makeStyles({
  root: {
    margin: '20px',
    minWidth: '250px',
    textAlign: 'center',
  },
  buttons: {
    padding: '20px',
    justifyContent: 'center',
  }
});
